# --- Build artifacts ---
node_modules
.next
dist
coverage
.turbo
*.tsbuildinfo

# --- OS noise ---
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# --- Editor noise ---
.idea/
.vscode/
*.swp
*.swo
*~

# --- Secrets / env ---
.env
.env.*
!.env.example
agents.env

# --- Local-only state files ---
.rough-loop.pause
.rough-loop.lock

# --- Claude Code transient worktrees (multi-GB sub-agent scratch dirs) ---
.claude/worktrees/
# personal permission overrides must not be shared via git
.claude/settings.local.json

# --- Vendor mirrors (synced via pnpm vendor:sync) ---
vendor/repos

# --- Runtime artifacts (large + per-run) ---
runtime-artifacts/*
!runtime-artifacts/.gitkeep
# world-cup shareables: price-stripped market structure cache, question list,
# model backbone, and the public forecast archive (market-blind by policy)
!runtime-artifacts/world-cup/
runtime-artifacts/world-cup/*
!runtime-artifacts/world-cup/polymarket/
!runtime-artifacts/world-cup/event-list/
!runtime-artifacts/world-cup/elo-table.json
!runtime-artifacts/world-cup/mc-results.json
!runtime-artifacts/world-cup/bracket-prediction.json
!runtime-artifacts/world-cup/run-ledger/
!runtime-artifacts/world-cup/reports/
!runtime-artifacts/world-cup/code-archive/
!runtime-artifacts/world-cup/fifa/
!runtime-artifacts/world-cup/fifa8-forecasts/
__pycache__/
run-error/
output/
.local/
.playwright-cli/

# --- E2E suite artifacts ---
e2e/artifacts/*
!e2e/artifacts/.gitkeep
e2e/e2e/

# --- Deploy adapters (auto-generated on link) ---
.vercel

# design-sync
.design-sync/sb-reference/
.design-sync/learnings/
.design-sync/.cache/
.design-sync/node_modules
.ds-sync/
ds-bundle/

# forecast engine design-handoff bundle (not versioned)
forecast-engine-handoff/
forecast-engine-handoff.zip
