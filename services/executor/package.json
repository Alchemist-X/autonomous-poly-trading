{
  "name": "@autopoly/executor",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./risk": {
      "types": "./src/risk.ts",
      "import": "./dist/risk.js",
      "default": "./dist/risk.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/services/executor/src/index.js",
    "preops:check": "pnpm --filter @autopoly/contracts build && pnpm --filter @autopoly/db build && pnpm --filter @autopoly/terminal-ui build",
    "preops:trade": "pnpm --filter @autopoly/contracts build && pnpm --filter @autopoly/db build && pnpm --filter @autopoly/terminal-ui build",
    "ops:check": "tsx src/ops/live-check.ts",
    "ops:trade": "tsx src/ops/live-check.ts --trade"
  },
  "dependencies": {
    "@autopoly/contracts": "workspace:*",
    "@autopoly/db": "workspace:*",
    "@autopoly/terminal-ui": "workspace:*",
    "@polymarket/clob-client": "5.8.0",
    "bullmq": "5.71.0",
    "drizzle-orm": "0.45.1",
    "dotenv": "17.2.3",
    "ethers": "5.7.2",
    "fastify": "5.8.2",
    "ioredis": "5.10.0"
  }
}
